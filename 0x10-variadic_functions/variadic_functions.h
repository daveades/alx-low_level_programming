#ifndef MAIN_H
#define MAIN_H

/* standard libraries */
#include <stdarg.h>
#include <stdio.h>

/* Function prototypes */
int sum_them_all(const unsigned int n, ...);
void print_numbers(const char *separator, const unsigned int n, ...);

#endif
